# Evita errores EPERM en Windows cuando npm intenta crear directorios en rutas inválidas
# (ej: prefix configurado como /usr/local que resuelve incorrectamente a C:\)
cache=${LOCALAPPDATA}/npm-cache
prefix=${APPDATA}/npm
